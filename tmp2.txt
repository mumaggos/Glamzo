src/pages/Partner.tsx:9:export default function Partner() {
src/pages/StripeSimulatedConnect.tsx:8:export default function StripeSimulatedConnect() {
src/pages/Signup.tsx:8:export default function Signup() {
src/pages/Signup.tsx:62:        const params = new URLSearchParams(window.location.search);
src/pages/StripeSimulatedCheckout.tsx:9:export default function StripeSimulatedCheckout() {
src/pages/StripeSimulatedCheckout.tsx:28:  const amount = Number(searchParams.get('amount') || 0);
src/pages/StripeSimulatedCheckout.tsx:32:  const creditAmount = Number(searchParams.get('creditAmount') || 0);
src/pages/StripeSimulatedCheckout.tsx:65:        throw new Error('Ocorreu um erro ao comunicar com a Gateway Stripe.');
src/pages/PartnerLogin.tsx:7:export default function PartnerLogin() {
src/pages/PartnerLogin.tsx:20:    const params = new URLSearchParams(window.location.search);
src/pages/PartnerLogin.tsx:49:        throw new Error('As credenciais do parceiro fornecidas estão incorretas.');
src/pages/PartnerLogin.tsx:60:        throw new Error('Falha ao validar privilégios do perfil.');
src/pages/Favorites.tsx:7:export default function Favorites() {
src/pages/Dashboard.tsx:29:export default function Dashboard() {
src/pages/Dashboard.tsx:90:  const [selectedAgendaDate, setSelectedAgendaDate] = useState<string>(new Date().toISOString().split('T')[0]);
src/pages/Dashboard.tsx:108:  const [manualDate, setManualDate] = useState(new Date().toISOString().split('T')[0]);
src/pages/Dashboard.tsx:147:      const svcPrice = selectedSvc ? Number(selectedSvc.price) : 0;
src/pages/Dashboard.tsx:151:      const duration = selectedSvc ? Number(selectedSvc.duration_minutes) : 30;
src/pages/Dashboard.tsx:155:      const endTimeStr = `${String(endH).padStart(2, '0')}:${String(endM).padStart(2, '0')}`;
src/pages/Dashboard.tsx:168:        throw new Error("Por favor, crie pelo menos um serviço no separador 'Serviços' antes de agendar manualmente.");
src/pages/Dashboard.tsx:249:        const d = new Date(b.starts_at || Date.now());
src/pages/Dashboard.tsx:252:          monthlyAccumulators[mName] += Number(b.price || b.total_price || 0);
src/pages/Dashboard.tsx:278:      const d = new Date(b.starts_at || Date.now());
src/pages/Dashboard.tsx:342:      const ctx = new AudioCtx();
src/pages/Dashboard.tsx:632:          booking_date: new Date().toISOString().split('T')[0],
src/pages/Dashboard.tsx:669:          bookingDate: new Date().toISOString().split('T')[0],
src/pages/Dashboard.tsx:791:        const blob = new Blob([rawSvg], { type: 'image/svg+xml' });
src/pages/Dashboard.tsx:940:                throw new Error(errText || "Invalid server response status code");
src/pages/Dashboard.tsx:946:              setTimeout(() => reject(new Error("Timeout de rede: Glamzo Pay webhook demorou mais que o esperado.")), 12000);
src/pages/Dashboard.tsx:998:                    started_at: new Date().toISOString(),
src/pages/Dashboard.tsx:1051:            await new Promise((resolve) => setTimeout(resolve, 1500));
src/pages/Dashboard.tsx:1170:        throw new Error(resData.error || 'Erro ao obter link Glamzo Pay.');
src/pages/Dashboard.tsx:1326:      const bDate = new Date(dateStr);
src/pages/Dashboard.tsx:1328:      return new Date() > bDate;
src/pages/Dashboard.tsx:1421:            price: Number(serviceForm.price),
src/pages/Dashboard.tsx:1422:            duration_minutes: Number(serviceForm.duration_minutes),
src/pages/Dashboard.tsx:1438:            price: Number(serviceForm.price),
src/pages/Dashboard.tsx:1439:            duration_minutes: Number(serviceForm.duration_minutes),
src/pages/Dashboard.tsx:1626:        valid_until: couponForm.valid_until ? new Date(couponForm.valid_until).toISOString() : null,
src/pages/Dashboard.tsx:1628:        created_at: new Date().toISOString()
src/pages/Dashboard.tsx:1640:            valid_until: couponForm.valid_until ? new Date(couponForm.valid_until).toISOString() : null,
src/pages/Dashboard.tsx:1873:    const spendInc = bk.booking_status === 'completed' ? Number(bk.total_price) : 0;
src/pages/Dashboard.tsx:1890:  const totalVolumeBruto = ledgers.reduce((sum, item) => sum + Number(item.amount_total || item.amount || 0), 0);
src/pages/Dashboard.tsx:1894:    return sum + Math.max(0, Number(item.glamzo_fee || 0));
src/pages/Dashboard.tsx:1897:  const totalReceivedVolume = ledgers.reduce((sum, item) => sum + Number(item.business_amount || item.amount_total || item.amount || 0), 0);
src/pages/Dashboard.tsx:1902:    .reduce((sum, item) => sum + Number(item.business_amount || item.amount_total || item.amount || 0), 0);
src/pages/Dashboard.tsx:1904:  const totalPayoutTransferred = payouts.filter(p => p.status === 'completed').reduce((sum, item) => sum + Number(item.amount || 0), 0);
src/pages/Dashboard.tsx:1921:    const diffTime = new Date(trialEndStr).getTime() - Date.now();
src/pages/Dashboard.tsx:1928:      const expiresAt = trialEndsAt ? new Date(trialEndsAt).getTime() : null;
src/pages/Dashboard.tsx:1934:      const createdAt = new Date(business.created_at).getTime();
src/pages/Dashboard.tsx:1942:      const expiresAt = trialEndsAt ? new Date(trialEndsAt).getTime() : null;
src/pages/Dashboard.tsx:1961:      const expiresAt = trialEndsAt ? new Date(trialEndsAt).getTime() : null;
src/pages/Dashboard.tsx:2241:                      const todayStr = new Date().toISOString().split('T')[0];
src/pages/Dashboard.tsx:2334:                    const todayStr = new Date().toISOString().split('T')[0];
src/pages/Dashboard.tsx:2599:                                  const d = new Date(selectedAgendaDate + 'T12:00:00');
src/pages/Dashboard.tsx:2612:                                  {new Date(selectedAgendaDate + 'T12:00:00').toLocaleDateString('pt-PT', { weekday: 'short', day: 'numeric', month: 'short' })}
src/pages/Dashboard.tsx:2632:                                  const d = new Date(selectedAgendaDate + 'T12:00:00');
src/pages/Dashboard.tsx:2644:                            {selectedAgendaDate !== new Date().toISOString().split('T')[0] && (
src/pages/Dashboard.tsx:2647:                                onClick={() => setSelectedAgendaDate(new Date().toISOString().split('T')[0])}
src/pages/Dashboard.tsx:2837:                              const baseDate = new Date(selectedAgendaDate + 'T12:00:00');
src/pages/Dashboard.tsx:2843:                                const targetDay = new Date(baseDate);
src/pages/Dashboard.tsx:2905:                              const baseDate = new Date(selectedAgendaDate + 'T12:00:00');
src/pages/Dashboard.tsx:2908:                              const firstDayOfMonth = new Date(year, month, 1);
src/pages/Dashboard.tsx:2909:                              const lastDayOfMonth = new Date(year, month + 1, 0);
src/pages/Dashboard.tsx:2915:                                gridCells.push(new Date(year, month, 1 - o));
src/pages/Dashboard.tsx:2918:                                gridCells.push(new Date(year, month, d));
src/pages/Dashboard.tsx:2922:                                gridCells.push(new Date(year, month + 1, nextDay));
src/pages/Dashboard.tsx:2928:                                const isToday = dateStr === new Date().toISOString().split('T')[0];
src/pages/Dashboard.tsx:3366:                                onChange={e => setServiceForm(prev => ({ ...prev, price: Number(e.target.value) }))}
src/pages/Dashboard.tsx:3375:                                onChange={e => setServiceForm(prev => ({ ...prev, duration_minutes: Number(e.target.value) }))}
src/pages/Dashboard.tsx:3548:                                const isSelected = offDaysArr.includes(String(dayIdx));
src/pages/Dashboard.tsx:3556:                                        newArr = offDaysArr.filter(d => d !== String(dayIdx));
src/pages/Dashboard.tsx:3558:                                        newArr = [...offDaysArr, String(dayIdx)];
src/pages/Dashboard.tsx:3611:                  return `${String(h).padStart(2, '0')}:${m}`;
src/pages/Dashboard.tsx:3821:                          {bookings.filter(b => b.booking_status === 'completed').reduce((sum, item) => sum + Number(item.total_price), 0).toFixed(2)} €
src/pages/Dashboard.tsx:4233:                          ⏳ Fim do período/Renovação: {new Date(trialEndsAt).toLocaleDateString('pt-PT')} ({trialDaysRemaining} dias restantes)
src/pages/Dashboard.tsx:4432:                        const originalPrice = Number(item.amount_total || item.amount || 0);
src/pages/Dashboard.tsx:4433:                        const platformFee = Number(item.glamzo_fee || 0);
src/pages/Dashboard.tsx:4434:                        const merchantProfit = Number(item.business_amount || 0);
src/pages/Dashboard.tsx:4435:                        const txDate = new Date(item.created_at || new Date()).toLocaleDateString('pt-PT');
src/pages/Dashboard.tsx:4442:                                  TX-{String(item.id).substring(0,8).toUpperCase()}
src/pages/Dashboard.tsx:4507:                            ? `Expira em: ${new Date(cp.valid_until).toLocaleDateString('pt-PT')}`
src/pages/Dashboard.tsx:5054:            <p className="text-xs font-semibold text-slate-800">{new Date(selectedInvoice.created_at).toLocaleDateString('pt-PT')}</p>
src/pages/Dashboard.tsx:5061:            <span>{Number(selectedInvoice.amount_total || selectedInvoice.amount || 0).toFixed(2)} €</span>
src/pages/Dashboard.tsx:5064:          {(Number(selectedInvoice.glamzo_fee || 0) < (Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) * 0.05)) && (
src/pages/Dashboard.tsx:5067:              <span>- {(Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) * 0.05).toFixed(2)} €</span>
src/pages/Dashboard.tsx:5077:            <span>{Number(selectedInvoice.business_amount || 0).toFixed(2)} €</span>
src/pages/AdminLogin.tsx:7:export default function AdminLogin() {
src/pages/AdminLogin.tsx:43:        throw new Error('As credenciais fornecidas não são autorizadas para o Painel Administrativo.');
src/pages/AdminLogin.tsx:67:        throw new Error('Falha na autenticação administrativa do sistema.');
src/pages/Account.tsx:12:export default function Account() {
src/pages/Account.tsx:192:    const params = new URLSearchParams(window.location.search);
src/pages/Account.tsx:428:        throw new Error(
src/pages/Account.tsx:550:                  const bookingDateFormatted = new Date(bk.booking_date + 'T00:00:00').toLocaleDateString('pt-PT', {
src/pages/Account.tsx:607:                        {bk.total_price ? Number(bk.total_price).toFixed(2) : '0.00'} €
src/pages/Account.tsx:735:                <span>{new Date(user.created_at).toLocaleDateString('pt-BR')}</span>
src/pages/Account.tsx:933:                      Desconto de {rw.value}.00 € • Expira em {new Date(rw.expires_at).toLocaleDateString('pt-PT')}
src/pages/Account.tsx:973:                      {new Date(rev.created_at).toLocaleDateString('pt-PT')}
src/pages/Explore.tsx:20:export default function Explore() {
src/pages/Explore.tsx:100:        const active = isPromoted && (!endsAt || new Date(endsAt).getTime() > now);
src/pages/Explore.tsx:254:    const currentDayIndex = new Date().getDay(); // 0 = Dom, 1 = Seg, ..., 6 = Sáb
src/pages/Explore.tsx:261:        const now = new Date();
src/pages/Explore.tsx:262:        const currentHours = String(now.getHours()).padStart(2, '0');
src/pages/Explore.tsx:263:        const currentMinutes = String(now.getMinutes()).padStart(2, '0');
src/pages/Explore.tsx:274:        const now = new Date();
src/pages/Explore.tsx:275:        const currentHours = String(now.getHours()).padStart(2, '0');
src/pages/Explore.tsx:276:        const currentMinutes = String(now.getMinutes()).padStart(2, '0');
src/pages/Login.tsx:8:export default function Login() {
src/pages/Login.tsx:21:    const params = new URLSearchParams(window.location.search);
src/pages/Login.tsx:50:      const params = new URLSearchParams(window.location.search);
src/pages/Home.tsx:13:export default function Home() {
src/pages/Home.tsx:52:            if (isPromoted && endsAt && new Date(endsAt).getTime() < now) {
src/pages/Home.tsx:165:    const queryParams = new URLSearchParams();
src/pages/PartnerSignup.tsx:12:export default function PartnerSignup() {
src/pages/PartnerSignup.tsx:94:        throw new Error('Falha ao registar credenciais. Verifique os dados digitados.');
src/pages/PartnerSignup.tsx:152:        throw new Error('Conta criada, mas ocorreu um erro ao inicializar o estabelecimento: ' + bizErr.message);
src/pages/PartnerSignup.tsx:157:        throw new Error('O registo foi criado, mas não conseguimos recuperar o identificador do estabelecimento.');
src/pages/Onboarding.tsx:13:export default function Onboarding() {
src/pages/Onboarding.tsx:118:        throw new Error(
src/pages/Admin.tsx:21:export default function Admin() {
src/pages/Admin.tsx:152:      display_order: Number(cmsDisplayOrder),
src/pages/Admin.tsx:155:      updated_at: new Date().toISOString()
src/pages/Admin.tsx:242:        throw new Error(uploadErr.message);
src/pages/Admin.tsx:352:          { id: 'p-01', amount: 45.00, created_at: new Date(Date.now() - 3 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:353:          { id: 'p-02', amount: 85.00, created_at: new Date(Date.now() - 5 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:354:          { id: 'p-03', amount: 120.00, created_at: new Date(Date.now() - 9 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:355:          { id: 'p-04', amount: 35.00, created_at: new Date(Date.now() - 12 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:356:          { id: 'p-05', amount: 155.00, created_at: new Date(Date.now() - 17 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:357:          { id: 'p-06', amount: 75.00, created_at: new Date(Date.now() - 21 * 24 * 3600 * 1000).toISOString() },
src/pages/Admin.tsx:358:          { id: 'p-07', amount: 210.00, created_at: new Date(Date.now() - 26 * 24 * 3600 * 1000).toISOString() }
src/pages/Admin.tsx:367:          { id: 'tc-801', customer_name: 'Parceiro Luxe Nails', business_name: 'Luxe Nails Porto', status: 'open', priority: 'high', description: 'Integração de Contas Stripe Connect falhou no checkout', created_at: new Date().toISOString() },
src/pages/Admin.tsx:368:          { id: 'tc-802', customer_name: 'Comerciante Barbearia', business_name: 'Barbearia da Linha', status: 'open', priority: 'medium', description: 'Não recebi o envio CTT de entrega do Tablet Terminal comodato', created_at: new Date().toISOString() }
src/pages/Admin.tsx:432:            expires_at: new Date(Date.now() + 365 * 24 * 3600 * 1000).toISOString()
src/pages/Admin.tsx:442:            started_at: new Date().toISOString(),
src/pages/Admin.tsx:443:            expires_at: new Date(Date.now() + 365 * 24 * 3600 * 1000).toISOString(),
src/pages/Admin.tsx:549:      const newTrialEnds = new Date(Date.now() + 14 * 24 * 3600 * 1000).toISOString();
src/pages/Admin.tsx:583:            started_at: new Date().toISOString(),
src/pages/Admin.tsx:912:  const totalVolumeGrossCalculated = paymentsList.reduce((sum, item) => sum + Number(item.amount), 0);
src/pages/Admin.tsx:921:    const sorted = [...paymentsList].sort((a, b) => new Date(a.created_at).getTime() - new Date(b.created_at).getTime());
src/pages/Admin.tsx:925:      const d = new Date(p.created_at).getDate();
src/pages/Admin.tsx:927:    }).reduce((sum, p) => sum + Number(p.amount || 0), 0);
src/pages/Admin.tsx:930:      const d = new Date(p.created_at).getDate();
src/pages/Admin.tsx:932:    }).reduce((sum, p) => sum + Number(p.amount || 0), 0);
src/pages/Admin.tsx:935:      const d = new Date(p.created_at).getDate();
src/pages/Admin.tsx:937:    }).reduce((sum, p) => sum + Number(p.amount || 0), 0);
src/pages/Admin.tsx:940:      const d = new Date(p.created_at).getDate();
src/pages/Admin.tsx:942:    }).reduce((sum, p) => sum + Number(p.amount || 0), 0);
src/pages/Admin.tsx:1201:                          const diff = new Date(sal.trial_ends_at).getTime() - Date.now();
src/pages/Admin.tsx:1264:                                  <span className="text-slate-300 block">{new Date(sal.created_at).toLocaleDateString('pt-PT')}</span>
src/pages/Admin.tsx:1265:                                  <span className="text-slate-500 text-[10px] block">{new Date(sal.created_at).toLocaleTimeString('pt-PT', {hour:'2-digit', minute:'2-digit'})}</span>
src/pages/Admin.tsx:1415:                          onChange={e => setPointsAllocVal(Number(e.target.value))}
src/pages/Admin.tsx:1723:                                onChange={(e) => setCouponDuration(Number(e.target.value))}
src/pages/Admin.tsx:1733:                                onChange={(e) => setCouponLimit(Number(e.target.value))}
src/pages/Admin.tsx:2205:                            onChange={(e) => setCmsDisplayOrder(Number(e.target.value))}
src/pages/Admin.tsx:2370:                                    <span className="text-slate-500">Última edição: {new Date(card.updated_at || card.created_at).toLocaleDateString('pt-PT')}</span>
src/pages/BusinessDetail.tsx:24:export default function BusinessDetail() {
src/pages/BusinessDetail.tsx:41:  const [qrPromptOpen, setQrPromptOpen] = useState(new URLSearchParams(window.location.search).get('source') === 'qrcode');
src/pages/BusinessDetail.tsx:103:    const customChatEvent = new CustomEvent('glamzo:open_chat', {
src/pages/BusinessDetail.tsx:178:      const reader = new FileReader();
src/pages/BusinessDetail.tsx:388:            const params = new URLSearchParams(window.location.search);
src/pages/BusinessDetail.tsx:710:                            {srv.price ? Number(srv.price).toFixed(2) : '0.00'} €
src/pages/BusinessDetail.tsx:857:                          {r.created_at ? new Date(r.created_at).toLocaleDateString('pt-PT') : 'Hoje'}
src/pages/BusinessDetail.tsx:1133:                      const isToday = new Date().getDay() === dayIdx;
src/pages/BusinessDetail.tsx:1259:                            <span className="text-[8px] text-slate-500 mt-1 font-mono">{new Date(msg.created_at).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</span>
src/scripts/inject-utility.ts:12:      const bDate = new Date(dateStr);
src/scripts/inject-utility.ts:14:      return new Date() > bDate;
src/scripts/find-bgs.ts:8:const uniqueBgs = [...new Set(bgs)];
src/scripts/find-bgs2.ts:14:console.log('bg-slate:', [...new Set(bgs)]);
src/scripts/find-bgs2.ts:15:console.log('text-slate:', [...new Set(texts)]);
src/scripts/find-bgs2.ts:16:console.log('border-slate:', [...new Set(borders)]);
src/scripts/fix-syntax.ts:30:"            <p className=\"text-xs font-semibold text-slate-800\">{new Date(selectedInvoice.created_at).toLocaleDateString('pt-PT')}</p>\n" +
src/scripts/fix-syntax.ts:37:"            <span>{Number(selectedInvoice.amount_total || selectedInvoice.amount || 0).toFixed(2)} €</span>\n" +
src/scripts/fix-syntax.ts:40:"          {(Number(selectedInvoice.glamzo_fee || 0) < (Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) * 0.05)) && (\n" +
src/scripts/fix-syntax.ts:43:"              <span>- {(Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) * 0.05).toFixed(2)} €</span>\n" +
src/scripts/fix-syntax.ts:53:"            <span>{Number(selectedInvoice.business_amount || 0).toFixed(2)} €</span>\n" +
src/scripts/remove-invoice-iife.ts:16:content = content.replace(/baseAmount \*/g, "Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) *");
src/scripts/remove-invoice-iife.ts:17:content = content.replace(/baseAmount/g, "Number(selectedInvoice.amount_total || selectedInvoice.amount || 0)");
src/scripts/remove-invoice-iife.ts:18:content = content.replace(/feeAmount/g, "Number(selectedInvoice.glamzo_fee || 0)");
src/scripts/remove-invoice-iife.ts:19:content = content.replace(/netProfit/g, "Number(selectedInvoice.business_amount || 0)");
src/scripts/remove-invoice-iife.ts:20:content = content.replace(/hasDiscount/g, "(Number(selectedInvoice.glamzo_fee || 0) < (Number(selectedInvoice.amount_total || selectedInvoice.amount || 0) * 0.05))");
src/utils/realtimeService.ts:9:    broadcastChannel = new BroadcastChannel(bcName);
src/utils/realtimeService.ts:34:        eventSource = new EventSource(streamUrl);
src/utils/realtimeService.ts:115:      created_at: new Date().toISOString(),
src/utils/realtimeService.ts:159:        updated_at: new Date().toISOString(),
src/utils/realtimeService.ts:186:      created_at: new Date().toISOString(),
src/utils/realtimeService.ts:199:        sessions[sIdx].updated_at = new Date().toISOString();
src/utils/realtimeService.ts:241:      created_at: new Date().toISOString(),
src/utils/realtimeService.ts:242:      updated_at: new Date().toISOString(),
src/utils/realtimeService.ts:270:      const entry = `[${new Date().toLocaleTimeString('pt-PT')}] ${formattedSender}: ${replyMessage}\n`;
src/utils/realtimeService.ts:272:      tickets[tIdx].updated_at = new Date().toISOString();
src/utils/realtimeService.ts:288:      tickets[tIdx].chat_history = currentHistory + `[${new Date().toLocaleTimeString('pt-PT')}] 👑 Suporte Glamzo: ${adminReply}\n[Ticket Resolvido]\n`;
src/utils/realtimeService.ts:289:      tickets[tIdx].updated_at = new Date().toISOString();
src/utils/realtimeService.ts:322:      timestamp: new Date().toISOString(),
src/utils/realtimeService.ts:511:        throw new Error('Falha no pedido da IA.');
src/utils/slugify.ts:13:    str = str.replace(new RegExp(from.charAt(i), 'g'), to.charAt(i));
src/utils/reviewsHelper.ts:84:    created_at: new Date().toISOString()
src/utils/reviewsHelper.ts:116:      const seedIds = new Set(SEED_REVIEWS.map(r => r.id));
src/utils/marketingHelper.ts:22:      return Number(data.credits ?? 40);
src/utils/marketingHelper.ts:49:  localStorage.setItem(`${CREDITS_STORAGE_PFX}${businessId}`, String(updated));
src/utils/marketingHelper.ts:61:  const endsAt = new Date(Date.now() + hours * 3600 * 1000).toISOString();
src/utils/marketingHelper.ts:76:  localStorage.setItem(`${CREDITS_STORAGE_PFX}${businessId}`, String(newCredits));
src/utils/marketingHelper.ts:98:      if (isPromoted && endsAt && new Date(endsAt).getTime() < Date.now()) {
src/utils/marketingHelper.ts:111:    if (endsAt && new Date(endsAt).getTime() < Date.now()) {
src/utils/marketingHelper.ts:228:    created_at: new Date().toISOString()
src/utils/communicationHelper.ts:48:    updated_at: new Date().toISOString()
src/utils/communicationHelper.ts:89:    created_at: new Date().toISOString()
src/utils/communicationHelper.ts:103:    sessions[index].updated_at = new Date().toISOString();
src/utils/communicationHelper.ts:270:    created_at: new Date().toISOString(),
src/utils/communicationHelper.ts:271:    updated_at: new Date().toISOString()
src/utils/communicationHelper.ts:306:    tickets[index].updated_at = new Date().toISOString();
src/utils/communicationHelper.ts:345:    created_at: new Date().toISOString()
src/utils/financeService.ts:81:    const originalCommission = Number((basePrice * feeRate).toFixed(2));
src/utils/financeService.ts:82:    const businessAmount = Number((basePrice - originalCommission).toFixed(2)); // Business strictly gets their full base price minus original commission
src/utils/financeService.ts:83:    const clientPays = Math.max(0, Number((basePrice - discount).toFixed(2)));
src/utils/financeService.ts:84:    const glamzoFee = Number((clientPays - businessAmount).toFixed(2)); // Glamzo absorbs the discount, resulting in a lower/negative fee
src/utils/financeService.ts:116:      created_at: new Date().toISOString()
src/utils/financeService.ts:134:      const expiresAt = new Date();
src/utils/financeService.ts:142:        started_at: new Date().toISOString(),
src/utils/financeService.ts:165:    const expiresAt = new Date();
src/utils/financeService.ts:179:      started_at: new Date().toISOString(),
src/utils/financeService.ts:201:    const initialBalance = Number(localStorage.getItem(`glamzo_partner_bal_${businessId}`) || '150.00');
src/utils/financeService.ts:202:    return Number(initialBalance.toFixed(2));
src/utils/financeService.ts:240:      created_at: new Date().toISOString()
src/utils/financeService.ts:263:      list[pIdx].processed_at = new Date().toISOString();
src/utils/financeService.ts:293:    return Number(pointsStr);
src/utils/financeService.ts:315:    const exp = new Date();
src/utils/financeService.ts:355:    return Number(creditsStr);
src/utils/financeService.ts:382:    const expires = new Date();
src/utils/financeService.ts:393:      created_at: new Date().toISOString(),
src/utils/financeService.ts:449:      created_at: new Date().toISOString()
src/utils/financeService.ts:527:        created_at: new Date().toISOString()
src/utils/financeService.ts:548:        created_at: new Date().toISOString(),
src/utils/financeService.ts:549:        expires_at: new Date(Date.now() + 12 * 3600 * 1000).toISOString()
src/utils/imageOptimizer.ts:12:  return new Promise((resolve, reject) => {
src/utils/imageOptimizer.ts:23:    const reader = new FileReader();
src/utils/imageOptimizer.ts:27:      const img = new Image();
src/utils/imageOptimizer.ts:88:        reject(new Error("Erro a carregar imagem para compressão: " + err));
src/utils/imageOptimizer.ts:93:      reject(new Error("Erro a ler ficheiro: " + err));
src/components/SupabaseSetupHelper.tsx:4:export default function SupabaseSetupHelper() {
src/components/Navbar.tsx:6:export default function Navbar() {
src/components/BottomSpacing.tsx:3:export default function BottomSpacing() {
src/components/BookingModal.tsx:19:export default function BookingModal({
src/components/BookingModal.tsx:33:  const totalServicesPrice = selectedServices.reduce((sum, s) => sum + Number(s.price), 0);
src/components/BookingModal.tsx:57:      const disc = Number((selectedService.price * 0.1).toFixed(2));
src/components/BookingModal.tsx:65:      const disc = Number((selectedService.price * 0.2).toFixed(2));
src/components/BookingModal.tsx:94:          const isValid = !matchedCoupon.valid_until || new Date(matchedCoupon.valid_until) > new Date();
src/components/BookingModal.tsx:98:              disc = Number((selectedService.price * (Number(matchedCoupon.discount_percent) / 100)).toFixed(2));
src/components/BookingModal.tsx:100:              disc = Math.min(Number(matchedCoupon.discount_value), selectedService.price);
src/components/BookingModal.tsx:119:        const isValid = !found.valid_until || new Date(found.valid_until) > new Date();
src/components/BookingModal.tsx:124:            disc = Number((selectedService.price * (Number(found.discount_percent) / 100)).toFixed(2));
src/components/BookingModal.tsx:126:            disc = Math.min(Number(found.discount_value), selectedService.price);
src/components/BookingModal.tsx:129:            disc = Number((selectedService.price * (Number(val) / 100)).toFixed(2));
src/components/BookingModal.tsx:132:            disc = Math.min(Number(val), selectedService.price);
src/components/BookingModal.tsx:155:          const value = Number(foundReward.value); // €5 or €10 discount
src/components/BookingModal.tsx:256:    const d = new Date();
src/components/BookingModal.tsx:281:    return `${String(h).padStart(2, '0')}:${String(m).padStart(2, '0')}`;
src/components/BookingModal.tsx:306:    const isToday = dateStr === new Date().toISOString().split('T')[0];
src/components/BookingModal.tsx:307:    const now = new Date();
src/components/BookingModal.tsx:343:              const offDaysArr = s.off_days.split(',').map(item => Number(item.trim()));
src/components/BookingModal.tsx:366:          const offDaysArr = selectedStaff.off_days.split(',').map(item => Number(item.trim()));
src/components/BookingModal.tsx:414:        throw new Error('Alerta anti-conflito: Este horário foi reservado por outro utilizador neste exato momento. Por favor escolha outra hora.');
src/components/BookingModal.tsx:433:      const discount = Number(couponDiscount || 0);
src/components/BookingModal.tsx:436:      const finalPriceToPay = Math.max(0, Number((basePrice + stripeTax - discount).toFixed(2)));
src/components/BookingModal.tsx:439:      const originalCommission = paymentMethod === 'stripe' ? Number((basePrice * feeRate).toFixed(2)) : 0;
src/components/BookingModal.tsx:440:      const businessAmount = paymentMethod === 'stripe' ? Number((basePrice - originalCommission).toFixed(2)) : finalPriceToPay; // Store receives full base rate minus commission for stripe, or 100% of final price for local cash!
src/components/BookingModal.tsx:441:      const glamzoFee = paymentMethod === 'stripe' ? Number((finalPriceToPay - businessAmount).toFixed(2)) : 0; // Glamzo absorbs the discount, resulting in a reduced/negative net fee representing the discount coverage on online, local is 0 fee!
src/components/BookingModal.tsx:498:            .update({ used: true, used_at: new Date().toISOString() })
src/components/BookingModal.tsx:528:            throw new Error(parsedErr);
src/components/BookingModal.tsx:556:            throw new Error('Nenhum link de checkout foi retornado');
src/components/BookingModal.tsx:560:          throw new Error(stripeErr.message || 'Serviço Glamzo Pay indisponível no momento. Por favor tente agendar selecionando "Pagar diretamente no local".');
src/components/BookingModal.tsx:644:                  {new Date(successBooking.booking_date).toLocaleDateString('pt', { weekday: 'long', day: 'numeric', month: 'long' })}
src/components/BookingModal.tsx:653:                <span className="text-slate-850 font-mono font-black text-sm">{Number(successBooking.total_price).toFixed(2)} €</span>
src/components/BookingModal.tsx:766:                            <span className="text-sm font-black text-rose-700 font-mono block">{Number(srv.price).toFixed(2)} €</span>
src/components/BookingModal.tsx:1058:                                <span>{Number(s.price).toFixed(2)} €</span>
src/components/ProtectedRoute.tsx:12:export default function ProtectedRoute({ children, allowedRoles }: ProtectedRouteProps) {
src/components/DashboardAssistant.tsx:15:export default function DashboardAssistant({ business, bookings, services, hours, staff }: AssistantProps) {
src/components/DashboardAssistant.tsx:59:      const dateObj = new Date(b.booking_date);
src/components/DashboardAssistant.tsx:95:    const nowMs = new Date().getTime();
src/components/DashboardAssistant.tsx:101:      const bDateMs = new Date(b.booking_date).getTime();
src/components/DashboardAssistant.tsx:103:      const price = Number(b.total_price) || 0;
src/components/DashboardAssistant.tsx:131:      const dateMs = new Date(b.booking_date).getTime();
src/components/DashboardAssistant.tsx:148:    const tomorrowStr = new Date(nowMs + msInDay).toISOString().split('T')[0];
src/components/GlamzoMessenger.tsx:18:export default function GlamzoMessenger() {
src/components/GlamzoMessenger.tsx:48:      const ctx = new AudioCtx();
src/components/GlamzoMessenger.tsx:444:                              {sess.updated_at ? new Date(sess.updated_at).toLocaleTimeString('pt-PT', { hour: '2-digit', minute: '2-digit' }) : ''}
src/components/GlamzoMessenger.tsx:490:                                <span>{new Date(msg.created_at).toLocaleTimeString('pt-PT', { hour: '2-digit', minute: '2-digit' })}</span>
src/components/ScrollToTop.tsx:8:export default function ScrollToTop() {
src/components/GlamzoLogo.tsx:12:export default function GlamzoLogo({
src/components/SecurityBadge.tsx:4:export default function SecurityBadge({ className = '' }: { className?: string }) {
src/components/DashboardMessages.tsx:8:export default function DashboardMessages({ businessId }: { businessId: string }) {
src/components/DashboardMessages.tsx:23:      const ctx = new AudioCtx();
src/components/DashboardMessages.tsx:146:                    {sess.updated_at ? new Date(sess.updated_at).toLocaleTimeString('pt-PT', { hour: '2-digit', minute: '2-digit' }) : ''}
src/components/DashboardMessages.tsx:208:                        <span>{new Date(msg.created_at).toLocaleTimeString('pt-PT', { hour: '2-digit', minute: '2-digit' })}</span>
src/App.tsx:28:function RouteLoader() {
src/App.tsx:39:export default function App() {
src/hooks/useAuth.tsx:21:export function AuthProvider({ children }: { children: React.ReactNode }) {
src/hooks/useAuth.tsx:78:            created_at: new Date().toISOString()
src/hooks/useAuth.tsx:119:          created_at: new Date().toISOString()
src/hooks/useAuth.tsx:172:          created_at: new Date().toISOString()
src/hooks/useAuth.tsx:205:        created_at: new Date().toISOString()
src/hooks/useAuth.tsx:316:      throw new Error('Supabase is not configured yet.');
src/hooks/useAuth.tsx:347:          created_at: new Date().toISOString()
src/hooks/useAuth.tsx:361:      throw new Error('Supabase is not configured yet.');
src/hooks/useAuth.tsx:381:      throw new Error('Supabase is not configured yet.');
src/hooks/useAuth.tsx:417:      throw new Error('Supabase is not configured yet.');
src/hooks/useAuth.tsx:433:    if (!user) throw new Error('Not authenticated');
src/hooks/useAuth.tsx:434:    if (!isSupabaseConfigured) throw new Error('Supabase is not configured');
src/hooks/useAuth.tsx:482:    throw new Error('useAuth must be used within an AuthProvider');
